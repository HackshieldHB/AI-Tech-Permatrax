# Permatrax Production Environment
# Generated from dev .env on 2026-05-19 (updated with full API/Web env surface)
# Server: permatrax.tech
# DO NOT COMMIT THIS FILE

# ── App (API + PM2) ──────────────────────────────────────────────────────────
# NODE_ENV: Nest/Next runtime mode; must be production on VPS
NODE_ENV=production
# PORT: API listen port (Nginx proxies api.permatrax.tech → this port)
PORT=3001
# FRONTEND_URL: Primary CORS origin; comma-separated if multiple web origins
FRONTEND_URL=https://permatrax.tech,https://www.permatrax.tech
# CORS_ALLOWED_ORIGINS: Explicit production CORS allowlist (overrides FRONTEND_URL when set)
CORS_ALLOWED_ORIGINS=https://permatrax.tech,https://www.permatrax.tech

# ── Database (Docker Compose postgres + Prisma) ─────────────────────────────
# POSTGRES_*: Used by docker-compose.prod.yml; keep in sync with DATABASE_URL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=permatrack
# DATABASE_URL: Prisma + API; localhost = Docker port mapped to host (127.0.0.1:5432)
DATABASE_URL=postgresql://postgres:password@localhost:5432/permatrack?schema=public&connection_limit=5&pool_timeout=20

# ── Redis (Docker Compose redis) ─────────────────────────────────────────────
# REDIS_PASSWORD: Required by docker-compose.prod.yml (appendonly + AUTH)
REDIS_PASSWORD=password
# REDIS_URL: JWT blacklist, Socket.IO adapter, Bull queues (PDF/mail), map cache
REDIS_URL=redis://:password@localhost:6379

# ── JWT / auth ───────────────────────────────────────────────────────────────
# JWT_SECRET: Access token signing (min 32 chars; rotate for production)
JWT_SECRET="KODE_RAHASIA_PERMATRAX_SANGAT_PANJANG_12345"
# JWT_REFRESH_SECRET: Refresh token validation + bcrypt-stored refresh hashes
JWT_REFRESH_SECRET="KODE_RAHASIA_REFRESH_PERMATRAX_SANGAT_PANJANG_12345"
# JWT_EXPIRES_IN / REFRESH_TOKEN_EXPIRES_IN: Documented in apps/api/.env.example (API uses 8h in code)
JWT_EXPIRES_IN=8h
REFRESH_TOKEN_EXPIRES_IN=7d

# ── File storage (local disk — not S3) ─────────────────────────────────────────
# UPLOAD_DIR: Relative to apps/api when PM2 cwd is apps/api → apps/api/uploads/
UPLOAD_DIR=./uploads
# FILE_BASE_URL: Public URL prefix returned after upload; must match Nginx /api/files
FILE_BASE_URL=https://api.permatrax.tech/api/files
# MAX_FILE_SIZE: Multer limit for /storage/upload (bytes); 52428800 = 50MB
MAX_FILE_SIZE=52428800

# ── AWS / S3 (legacy — optional; StorageService uses local filesystem only) ───
# AWS_REGION=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_BUCKET_NAME=
# AWS_S3_BUCKET=
# S3_ENDPOINT=

# ── SMTP / email (optional — ISP email, procurement PO, Bull mail queue) ───────
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@permatrax.com
# SMTP_SECURE=false
# PROCUREMENT_FROM_EMAIL: Sender for procurement PO emails (fallback: SMTP_FROM → SMTP_USER)
PROCUREMENT_FROM_EMAIL=procurement@permatrax.id

# ── Error monitoring (optional) ───────────────────────────────────────────────
# SENTRY_DSN: API server-side Sentry (leave empty to disable)
SENTRY_DSN=

# ── SSL / setup scripts ───────────────────────────────────────────────────────
# CERTBOT_EMAIL: Let's Encrypt registration (full-setup.sh)
CERTBOT_EMAIL=admin@permatrax.tech

# ── Next.js / Web (build-time + runtime; export before pnpm build in apps/web) ─
# NEXT_PUBLIC_API_URL: Browser API base URL (must be public HTTPS in production)
NEXT_PUBLIC_API_URL=https://api.permatrax.tech/api
# NEXT_PUBLIC_FILES_URL: File CDN/base URL (defaults to API host + /api/files if unset)
NEXT_PUBLIC_FILES_URL=https://api.permatrax.tech/api/files
# NEXT_PUBLIC_APP_NAME / VERSION: UI branding (optional)
NEXT_PUBLIC_APP_NAME=PermaTrax
NEXT_PUBLIC_APP_VERSION=1.0.0
# NEXT_PUBLIC_MAPBOX_TOKEN: MapLibre/Mapbox map tiles (required for GIS map features)
NEXT_PUBLIC_MAPBOX_TOKEN=your-mapbox-token
# NEXT_PUBLIC_SENTRY_DSN: Browser Sentry (leave empty to disable)
NEXT_PUBLIC_SENTRY_DSN=
# SENTRY_DSN: Next.js server/edge Sentry (can match NEXT_PUBLIC_SENTRY_DSN or stay empty)
# INTERNAL_API_URL: Optional SSR refresh proxy to API (defaults to localhost:3001)
# INTERNAL_API_URL=http://127.0.0.1:3001
# NEXT_PUBLIC_DISABLE_API_HEALTH_BANNER=true
# NEXT_PUBLIC_DEBUG=false
# NEXT_PUBLIC_DEBUG_API=false

# ── PM2 (optional) ────────────────────────────────────────────────────────────
# PM2_INSTANCES: API cluster worker count (default max = all CPU cores)
# PM2_INSTANCES=4
